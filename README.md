# Selenium UI Tests

Automated browser UI tests — **Python + Selenium WebDriver + pytest**.

## Structure
- `tests/test_ui.py` — UI test scenarios
- `requirements.txt` — dependencies

## Run
```bash
pip install -r requirements.txt
pytest -v
```
